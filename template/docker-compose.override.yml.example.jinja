services:
  app:
    # platform: linux/arm64

  db:
    # platform: linux/amd64
{%- if include_vite %}

  node:
    # platform: linux/arm64/v8
{%- endif %}

  tailwind:
    # platform: linux/amd64
{%- if include_worker %}

  worker:
    # platform: linux/amd64
{%- endif %}
